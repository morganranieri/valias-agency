quote: $22.8k 
due: 2026-Jul-15
approved: no

Question for client (
  mark the roll each session?
  how can a student be in the attendance but not paid/enrolled? 
  can you live without notes in the attendance, provided we put in medical info? 
)

Question for development (
  should we include the location of the class in the model?  or see how this goes without it and then adapt? 
)

Scope (
  "Replace the attendance spreadsheet"
  Tutors login and view classes they are assigned to in the present or upcoming term
  Tutors select a class and mark attendance for each student at the required frequency
  A program has a main class
  Admin creates additional classes for a program (i.e. beginner, advanced)
  Admin moves students from the main class to another
  Admin assigns one or more tutors to a class
  Admin can produce a report on all student attendance (absent, present, null) for a given year and term 
)

Out of scope (
  students who aren't enrolled - new student arrived in class - adding them to the roll - best case, get the contact details of the parents and contact them 
)

Model (
  tutors (user account)
  program.class (has many)
  program.class.tutors (has many)
  prorgam.class.student (has many)
  program.class.student.date.attended (bool)
)

View (
  admin/tutors/new
  admin/tutors/edit
  admin/tutors/delete

  admin/program/class/new
  admin/program/class/edit
  admin/program/class/delete
  admin/program/class/tutors - shows a list of all tutors and user can bump one up into the assigned section

  admin/program/students - with a drop-down next to each student for class allocation
  
  tutor/dashboard
  tutor/classes
  tutor/classes/1 - can see sessions with dates (which is start date + (7 * session number))
  tutor/classes/1/session/1 - can see students, medical info, school class/grade and mark the roll (checkbox) as a form that's submitted once. 
  
  admin/reports/attendance

)

Controller (
  
)