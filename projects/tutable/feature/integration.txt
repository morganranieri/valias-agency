Subdomain to go to a different server 
 add DNS record for a subdomain 

Push for Heroku due to time 


On premesis out of scope 

Embed tutable via iframe 

  Other website embedding options (
    Option 2 “Widget” (JS + API)
    Option 3 “Same-site” reverse proxy / custom subdomain 
  )